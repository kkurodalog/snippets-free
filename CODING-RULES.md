# 掲載基準と実装規範

このリポジトリに載せるパーツが満たす基準と、その実現方法をまとめた1枚。
**全カテゴリ共通**の規範であり、カテゴリ固有の事情は各カテゴリの `coding-rules.md` に置く。

---

## 1. 掲載基準（4項目）

掲載しているパーツは、次の4項目を確かめてから公開する。

| # | 項目 | 内容 |
|---|---|---|
| (1) | **プログレッシブエンハンスメント** | JavaScript 無効・機能非対応の環境でも、情報の欠落とレイアウトの破壊が起きない |
| (2) | **動きの軽減に対応** | `prefers-reduced-motion: reduce` で意図どおり止まる、または最終状態になる |
| (3) | **キーボード操作** | 操作要素にキーボードで到達でき、フォーカスが見える |
| (4) | **実挙動の実測** | 静的なコードレビューだけで終わらせず、実ブラウザで動きを計測する |

満たしていない項目があるパーツは、**その項目を明示する**。黙って落とさない。

---

## 2. プログレッシブエンハンスメント

- 初期状態は **JavaScript 無しで成立する形**で書く。JavaScript は「格上げ」に使う
- 初期隠蔽は `.js` スコープに限定する。`.no-js` → `.js` の書き換えは `<head>` 内のインラインスクリプトで先に済ませ、ちらつき（FOUC）を出さない
- 押せない UI を残さない。JavaScript が要る操作要素は、無効時に出さない
- 機能検出は `CSS.supports()` と `@supports` を**同じ判定キー**で書く。片方だけ変えると二重駆動が起きる

---

## 3. 動きの制御

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}
```

- 出現演出は `reduce` 時に**即時最終状態**にする（`opacity: 1` / `transform: none` / `transition: none` / `pointer-events: auto` / `will-change: auto`）
- ⚠️ **情報を提示する UI は「即時最終状態」を踏襲しない。** 読了率バーを満タン固定や非表示にすると誤った情報になる。**操作に直結する追従は残し、補間の transition だけを切る**
- 動画のプレビューは `reduce` 時に自動再生せず、静止画で止める

---

## 4. CSS 詳細度

- **詳細度の設計で解決する。`!important` で逃げない**
- 子孫セレクタで状態を分けたら、**最終状態の詳細度を同点以上に保ち、ソース順で後ろに置く**。下げると前の指定が上書きし返して状態が戻らなくなる
- 属性セレクタで「対象そのもの」を指すときは、**対象を表す属性を AND して絞る**。素の属性1つだけで書くと、その属性を親に付けたときに親自身にも当たる
- 不可視の間は `pointer-events: none`。可視になったら `auto` に戻す
- 状態は**1つの真実の源**（`.is-*` クラス1つ）に集約し、見た目の切り替えは CSS に寄せる

---

## 5. 名前空間（カテゴリ一覧に複数パーツが同居するための規約）

カテゴリ一覧ページは、同じ画面に**同一カテゴリのパーツを最大7件**並べる。
名前が衝突すると後勝ちで上書きされ、**別のパーツの見た目が壊れる**。

| 対象 | 規約 | 例 |
|---|---|---|
| **クラス名** | 役割の接頭辞 ＋ パーツ slug。`l-`（レイアウト）/ `c-`（部品）/ `p-`（ページ固有）/ `u-`（ユーティリティ） | `c-accordion__item` |
| **カスタムプロパティ** | `--{パーツ slug}-{用途}`。⚠️ **`:root` に定義しない。パーツのルート要素に定義する** | `.c-accordion { --accordion-bg: #fff; }` |
| **id 属性** | `{パーツ slug}-{用途}-{連番}` で一意にする。`aria-controls` / `aria-labelledby` の参照先になるため衝突が実害になる | `accordion-panel-1` |
| **id 属性（数字で始まる場合）** | パーツのディレクトリ名は `NNN_slug` 形式のため、そこから作る id は**数字で始まる**。HTML の id として妥当で `href="#…"` も動くが、**CSS セレクタと `querySelector()` ではエスケープが要る**。JS から引くときは `document.getElementById(id)` を使う（`'#' + id` を組み立てない） | `001_js-toggle-actions` |
| **JavaScript のフック** | `data-{機能}` 属性を起点にする。見た目のクラスをフックに使わない | `data-accordion` |
| **JavaScript のフック（クラスで持つ場合）** | `js-` 接頭辞。スタイルを当てない | `js-accordion-toggle` |

- 親と子でフックを分けてよい（グループ監視は親、対象は子）
- **1つのパーツが定義する名前は、そのパーツの slug から辿れること**

---

## 6. 掲載範囲マーカー（コピーできる範囲を示す印）

カタログのカードから開くコード表示は、**デモの実ファイルをその場で読んで表示する**。
コピー用に別の写しを持たないため、**ファイルのどこからどこまでが「貼って使える範囲」か**をファイル自身が示す。
その印がマーカーである。

### 6-1. 記法

| 言語 | 開始 | 終了 |
|---|---|---|
| HTML | `<!-- snippet:start -->` | `<!-- snippet:end -->` |
| CSS | `/* snippet:start */` | `/* snippet:end */` |
| JavaScript | `/* snippet:start */` | `/* snippet:end */` |

- **トークンは3言語とも `snippet:start` / `snippet:end` の同一文字列**。違うのはコメントの囲みだけ
- **JavaScript でも行コメント（`//`）を使わない。** ブロックコメントに揃える
- **日本語を含めない。** 記法そのものが翻訳の対象にならない形にする

### 6-2. 置き方

| # | 規約 |
|---|---|
| 1 | **マーカーはその行に単独で置く。** 同じ行に他の記述を混ぜない |
| 2 | **1ファイルに1組だけ。** 入れ子・複数組・片側だけは、いずれも不正とする |
| 3 | **範囲の中に解説コメントを書かない。** そのままコピーされ、貼った先に残る |
| 4 | **範囲だけを貼って動くこと。** 範囲の外にある指定に依存させない |
| 5 | **表示とコピーは同じ範囲にする。** 見えているものがそのままコピーされる状態を崩さない |
| 6 | **デモが持つ `index.html` / `assets/css/style.css` / `assets/js/script.js` は、いずれも掲載範囲を含むファイルである。** デモのページ枠だけに使う（＝マーカーで囲む範囲を1行も持たない）スクリプトやスタイルを、この3つのパスに置かない。置きたい場合は別のファイル名にする |

> 6 の理由。カードのボタン `[HTML][CSS][JS]` はカード定義の `files[]` から作られ、**`files[]` と実ファイルの有無は両方向で突き合わせている**（宣言したのに無い／あるのに宣言が無い、のどちらも生成を止める）。ページ枠だけの `assets/js/script.js` を置くと、**掲載範囲を持たないファイルに対して `[JS]` ボタンを出すか、突合を落とすか**の二択になる。**範囲外に依存させない**（規約4）という原則をファイルの置き方の側でも守れば、この二択そのものが発生しない。

### 6-3. 判定

判定の実装は `assets/js/snippet-marker.js` の1本だけで、ブラウザ側の表示と検査の両方がこれを使う。
検査は `npm run check:markers` で行い、次のとおり扱う。

- 1組そろっている … 合格
- 片側だけ・入れ子・2組以上 … 不合格
- 範囲の中に日本語の行コメントがある … 不合格
- マーカーが1つも無い … 保留（埋め込み前のファイル）

**保留は、同じカテゴリのファイルに1本でもマーカーが入った時点で不合格へ切り替わる。**
埋め込みはカテゴリ単位で進むため、1本入っていて残りが入っていない状態は取りこぼしである。

### 6-4. 取り出したあとの整形

- 範囲の共通の字下げは、表示するときに外す（深い位置にあっても貼った先で先頭が揃う）
- 範囲の前後の空行は落とす
- **いずれも表示側の処理であり、実ファイルは書き換えない**

---

## 7. JavaScript の作法

- **ES modules で書き、バンドルしない**
- イベントは委譲を基本にする。要素が増えてもリスナーが線形に増えない形にする
- **数値の属性値は `Number.isFinite(x) && x >= 0` で検証する**。`Number() || 0` は NaN と負値を素通りさせる
- 非対応環境のフォールバックで、**元の演出を無理に再現しない**。情報が伝わる最小の形に落とす
- 例外時に画面が空にならないようにする。取得に失敗したら、代わりの導線を示す

---

## 8. 実挙動の検証（視覚効果を伴うパーツは必須）

**静的なコードレビューだけでは、実挙動の不具合を捉えきれない。**
コードが規範どおりでも、ブラウザの実挙動（スクロールポートの解決・タイムラインの塗りつぶし・合成と再描画）でしか出ない不具合がある。

### 検証観点（最低限）

- (1) 効果がスクロール等に連動して意図どおり駆動し、**範囲の端と超過後も保持される**か
- (2) `prefers-reduced-motion: reduce` で意図どおり無効化・最終状態になるか
- (3) JavaScript 無効・機能非対応で情報の欠落とレイアウトの破壊が起きないか
- (4) キーボードで到達・操作でき、フォーカスが見えるか

### 手順

- ヘッドレスブラウザを一時ディレクトリに用意し、`getComputedStyle()` 等で**実値を計測する**
- **ローカルと公開 URL の両方**で確認する
- 検証用の一時ディレクトリは確認後に削除する

### 横スクロールの検査

ページ本体に横スクロールを出さない（横に送るのはコード枠・chip 列など、枠の内側に閉じたものだけ）。

- **測る条件は「全ページ × 全幅 × 既定フォント 16 / 32px」を掛け合わせる。** 1ページだけで幅を細かく刻んでも網羅にならない。**中身の幅はページごとに違う**ため、いちばん中身の多いページで出る
- **400% ズーム相当（1280px 幅を 320px 相当まで縮める）を必ず含める。** これは Reflow の標準的な検証条件である
- **判定は2つとも見る。** `document.documentElement.scrollWidth > clientWidth` と、実際に右端まで送ったあとの `scrollLeft`。⚠️ **前者は `overflow-x: auto` の内側のはみ出しを数えない**（枠内スクロールでは鳴らない）。**鳴ったら本物として扱う**

#### `overflow-x: auto` の枠に `position: relative` を付ける

**横に送る枠（`overflow-x: auto`）には `position: relative` を付ける。**

`overflow` がクリップするのは「自分の中身」だけである。枠が `position: static` のままだと、**枠の中にある `position: absolute` な子**（読み上げ専用テキスト・バッジ・ツールチップ等）の包含ブロックが**枠の外側**になり、クリップの対象から外れる。はみ出しはそのままページのスクロール領域へ出て、**ページ全体が横に動く**。

- `html` / `body` に `overflow-x: hidden` を当てても消えない。**効くのは包含ブロックを作るほうだけである**
- 枠の中に絶対配置の子が「いま無い」場合でも付けておく。**後から1つ足した時点で再発する**

### 実測でしか出なかった不具合の「型」

コードを読んでも見えず、動かして初めて出るのは次の3つの型である。

- **駆動元が意図した要素になっていない** — スクロール連動の効果が、想定と違う要素を基準に計算されている
- **範囲の外で状態が保持されない** — 効果の適用範囲を抜けた後に初期値へ戻る
- **測った条件が穴を外していた** — 不具合は最初から出ていたのに、**測った組み合わせがそこを通っていない**。条件を掛け合わせる軸が2つ以上あるとき、1軸だけ細かく刻むと網羅したように見える

具体例（どのパーツで、何が原因で、どう直したか）は、そのカテゴリの `coding-rules.md` に置く。
**同じ実例を2箇所に書かない。** 片方を直したときにもう片方が古くなる。

---

## 9. カテゴリ固有の規範

カテゴリ特有の地雷は、そのカテゴリの `coding-rules.md` に置く。

- `components/scroll-animation/coding-rules.md`

---

## 10. ライセンス

コードは MIT。**素材（画像・動画）は MIT の対象外**。詳細は `LICENSE` を参照。
